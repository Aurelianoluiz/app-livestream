class Scene { final String id; final String name; const Scene({required this.id, required this.name}); }
class Product { final String id; final String name; const Product({required this.id, required this.name}); }
class Offer { final String id; final String title; const Offer({required this.id, required this.title}); }
class Transmission { final String id; final String title; const Transmission({required this.id, required this.title}); }
class Overlay { final String id; final String name; const Overlay({required this.id, required this.name}); }
class Settings { final String studioName; const Settings({this.studioName='LIVE STUDIO ASR'}); }
