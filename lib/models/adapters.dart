// V1 intentionally uses plain Dart models and string collections in Hive.
// No generated adapters are required. Keep this registry as the extension point
// for future typed Hive adapters when the domain models become persistent objects.
void registerAdapters() {}
